export enum ThemeName {
  DEFAULT = 'default',
  OCEAN = 'ocean'
}

export enum ThemeMode {
  DARK = 'dark',
  LIGHT = 'light'
}

export enum ThemeActionType {
  SET_THEME = 'THEME_SET',
  SET_MODE = 'THEME_SET_MODE'
}
