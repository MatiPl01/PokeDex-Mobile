import styled from 'styled-components/native';
import { absoluteFill } from '@styles/shared';

export const Wrapper = styled.View`
  flex: 1;
`;

export const SectionsScrollView = styled.ScrollView`
  ${absoluteFill};
`;
