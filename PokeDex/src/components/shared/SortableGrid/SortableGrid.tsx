import React, { PropsWithChildren } from 'react';

type SortableGridProps = PropsWithChildren<{}>;

const SortableGrid: React.FC<SortableGridProps> = ({ children }) => {
  return children;
};

export default SortableGrid;
