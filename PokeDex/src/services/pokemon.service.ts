import camelize from 'camelize';
import { PokemonListResponse } from './../store/favorites/favorites.types';


export const fetchNextPokemon = (
  offset: number,
  limit: number
): PokemonListResponse => {
  axios.get()
};
